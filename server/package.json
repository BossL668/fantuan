{
  "name": "fantuan-server",
  "version": "1.0.0",
  "description": "Backend server for Fantuan fan community platform",
  "main": "index.js",
  "scripts": {
    "start": "cd ../client && npm install && npm run build && cd ../server && node index.js",
    "dev": "nodemon index.js",
    "build": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "dotenv": "^16.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
} 